/**
 * Custom Hooks Structure
 *
 * All data fetching MUST go through hooks following the pattern:
 * Component → Custom Hook → Service → Apollo → Backend
 */

export { useAssets } from './useAssets';
export { useLoans } from './useLoans';

// Add more hooks as needed per module
